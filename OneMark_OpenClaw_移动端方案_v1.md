# OneMark / 一藏 方案设计（v2）

## 1. 产品基础信息
- 产品名：OneMark
- 中文名：一藏
- Slogan：Never lose a link again.
- 核心定位：通过 OpenClaw 自动采集各平台收藏内容，上云后由 OneMark 在移动端统一检索、分类和访问。

## 2. 目标与边界
### 2.1 目标
1. 使用 OpenClaw 自动化采集收藏数据，不依赖用户每次手动操作。
2. 采集结果进入 OneMark 云端，统一标准化、分类和索引。
3. 在移动端提供高效浏览、搜索、跳转和管理体验。

### 2.2 边界
1. OneMark 不代替平台账号体系，采集动作由 OpenClaw 执行。
2. 首版采用云端主存储，移动端仅做缓存与离线加速。
3. 不做平台写回（不回写到原平台收藏夹）。

## 3. 总体架构（OpenClaw + Cloud + Mobile）
### 3.1 架构角色
1. OpenClaw Runner（桌面守护进程）：按计划自动拉起，自动打开目标网页并采集。
2. OneMark Ingest API（云端）：接收采集数据包、鉴权、入库。
3. OneMark Processing（云端）：去重、LLM 分类、索引构建。
4. OneMark Mobile（移动端）：展示、查询、手动修正标签。

### 3.2 数据流
1. 用户首次完成平台登录状态准备和 OneMark 账号绑定。
2. 定时任务每日触发 OpenClaw Runner，自动打开目标平台收藏页并执行采集。
3. OpenClaw 导出 `onemark_export.json` 后自动上传 OneMark 云端，无需用户手动传输。
4. 云端执行数据合法性校验、去重、LLM 打标、索引更新。
5. 移动端定时拉取增量数据并更新本地缓存。
6. 用户在 App 内进行自然语言查询，系统返回目标链接列表；用户可手动改标签并回写云端。

## 4. 数据模型（导入标准）
```json
{
  "version": "1.0",
  "source": "openclaw",
  "platform": "xiaohongshu",
  "exported_at": "2026-02-13T10:00:00Z",
  "items": [
    {
      "external_id": "6985a97e000000001a01e3bc",
      "title": "示例标题",
      "url": "https://www.xiaohongshu.com/explore/6985a97e000000001a01e3bc",
      "cover_url": "https://.../cover.webp",
      "author": "作者名",
      "collected_at": "2026-02-12T08:00:00Z",
      "platform": "xiaohongshu",
      "raw": {}
    }
  ]
}
```

必填字段：`external_id`, `title`, `url`, `platform`  
建议字段：`cover_url`, `author`, `collected_at`, `raw`

## 5. 导入与同步策略
### 5.1 导入模式
1. 全量导入：首次建立收藏库。
2. 增量导入：后续仅新增/更新。

### 5.2 冲突与去重
1. 主键：`platform + external_id`。
2. 兜底：`canonical_url`。
3. 标题变化视为更新，不新建。

### 5.3 同步频率
1. 默认每日定时自动同步（建议凌晨低峰时段）。
2. 支持后台失败重试（例如 3 次指数退避）。
3. 保留“立即同步”仅用于异常修复，不作为主流程。

## 6. 移动端展示模式（核心）
### 6.1 首页（Search First）
1. 顶部全局搜索框：自然语言输入（如“我想做饭”）。
2. 查询由 LLM + 检索引擎联合处理，返回最相关链接列表。
3. 快捷入口：最近导入、高频标签、最近访问。

### 6.2 收藏列表页
1. 视图切换：卡片流 / 紧凑列表。
2. 筛选维度：平台、标签、时间、是否有封面。
3. 排序：最近收藏、最近访问、相关度。

### 6.3 详情页
1. 展示标题、封面、来源平台、原始链接。
2. 操作：打开原文、复制链接、加入集合、修改标签。

### 6.4 智能查询页
1. 输入一句话，调用大模型进行意图识别和语义召回。
2. 输出结果包含：命中链接、命中理由、平台标记、置信度。
3. 支持“这不是我要的”反馈，反向优化排序。

## 7. 视觉与品牌规范（按你给定方向）
1. 视觉语言：Gemini 风格设计语言（极简结构、卡片分层、柔和光效）。
2. 主题：默认深色模式。
3. 品牌主色：电蓝 + 霓虹紫，保留图标光晕质感。
4. 图标：使用你提供的勾选书签图形作为 App Icon 主体。
5. 文案：
   - 英文：OneMark
   - 中文：一藏
   - Slogan：Never lose a link again.

## 8. 合规与安全（云端主存储）
1. OneMark 不存储平台账号密码。
2. OpenClaw 在用户侧执行采集，OneMark 仅接收用户授权的数据上传。
3. 数据存储在云端（加密存储 + 传输 TLS + 细粒度访问控制）。
4. 提供一键删除云端数据、一键导出数据能力。
5. 首次授权弹窗明确告知：数据来源、用途、保存位置、删除路径。

## 9. 技术选型建议
1. App：Flutter（单代码 iOS/Android）。
2. 云端 API：FastAPI 或 NestJS。
3. 云数据库：PostgreSQL（主数据）+ Redis（任务与缓存）+ 对象存储（封面缓存）。
4. 检索：FTS + 向量检索（pgvector）。
5. 模型层：LLM 分类与查询重排服务。
6. 传输协议：OpenClaw 上传 JSON（主），CSV（备）。

## 10. 里程碑
1. M1（1-2周）：OpenClaw 自动采集与云端入库闭环。
2. M2（2-3周）：移动端列表、详情、筛选和跳转。
3. M3（2周）：LLM 分类、自然语言查询、标签手动修正回写。
4. M4（1周）：Gemini 风格 UI 打磨与灰度发布。

## 11. 验收标准
1. 首发支持平台：小红书、B站、抖音。
2. 每日定时自动同步成功率 >= 98%。
3. 单次导入 >= 500 条收藏，导入成功率 >= 99%。
4. 智能查询返回可点击链接列表，P95 < 2.5s（含模型调用）。
5. 可正确展示标题与封面（字段非空时）。

## 12. 已按你要求锁定项
1. OpenClaw 自动拉起，不要求用户每次手动打开。
2. 每日定时自动传输，不走手动文件传递主流程。
3. 分类打标使用大模型，并支持用户在 App 端手动修改。
4. 查询走大模型输出用户想要的链接结果。
5. 数据上云，不采用“本地优先”作为首版主策略。
6. 首发平台为小红书、B站、抖音。
7. UI 按 Gemini 风格与设计语言推进。

